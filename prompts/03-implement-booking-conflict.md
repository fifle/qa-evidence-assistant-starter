# Prompt 03 — Implement booking conflict detection

Ask your AI assistant to implement **one bounded change**: improve or verify booking conflict detection in `signalRules.ts`.

Requirements:

- Use `samples/har/checkout-409-conflict.sanitized.har` as the primary fixture
- Add or update tests in `signalRules.test.ts`
- Do not add a backend or external API calls
- Run `npm run test` after changes
