# Prompt 05 — Harden the tool

Ask your AI assistant to improve:

- Redaction coverage (headers, URLs, response bodies)
- Validation and user-facing warnings for invalid HAR
- Missing evidence detection in `reportBuilder.ts`
- Unit tests for edge cases

Run `npm run test` and `npm run build` when done.
